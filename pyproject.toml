[project]
name = "agentic-renderdoc"
version = "1.0.0"
description = "MCP server for AI-assisted graphics debugging with RenderDoc"
requires-python = ">=3.10"
license = "MIT"
authors = [
    {name = "Eden Labs"},
]
keywords = ["renderdoc", "mcp", "gpu", "graphics-debugging"]
classifiers = [
    "Development Status :: 5 - Production/Stable",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3",
    "Topic :: Software Development :: Debuggers",
]
dependencies = [
    "mcp>=2,<3",
    "Pillow>=10.0",
]

[project.optional-dependencies]
dev = [
    "build>=1.2",
    "hatchling>=1.27",
    "mcp[cli]>=2,<3",
    "pytest>=8",
]

[project.urls]
Repository = "https://github.com/EdenLabs/agentic-renderdoc"

[project.scripts]
agentic-renderdoc = "server.__main__:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
# Both packages need to ship in the wheel. The server is the MCP entry
# point; the extension is loaded into qrenderdoc directly AND its
# headless variant is spawned as a subprocess by the server
# (`python -m extension.headless ...`), so it has to be importable
# from the same Python that runs the server.
packages = ["src/server", "src/extension"]

[tool.pytest.ini_options]
markers = [
    "windows_renderdoc: opt-in smoke tests that require RenderDoc 1.45 and a real .rdc capture",
]
